# NexStructure

A .NET 10 API solution template organized into API, Application, Domain, and Infrastructure projects.

```bash
dotnet new install NexStructure.Template
dotnet new nexstructure -n MyApi
```

Requires **.NET 10**.
